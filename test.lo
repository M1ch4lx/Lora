vec = (1, 2, 3);

x, y, z = vec;

x, y, z = (x * 2, y * 2, z * 2);

vec = (x, y, z);

function add_vec(vec1, vec2)
{
    x1, y1, z1 = vec1;
    x2, y2, z2 = vec2;
    return (x1 + x2, y1 + y2, z1 + z2);
}

result = add_vec(vec, (4, 2, 0));